# chap-models check (2026-05-04T20:27:54+00:00 → 2026-05-04T20:56:16+00:00)

**21 pass · 7 fail · 0 skip** of 28 repos.

| Repo | Style | Period | Covariates | Status | Cause / note | Suggested fix | Time |
| --- | --- | --- | --- | --- | --- | --- | ---: |
| [`auto_arima`](https://github.com/chap-models/auto_arima) | mlproject | month | — | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 11.9s |
| [`auto_arima_chapkit`](https://github.com/chap-models/auto_arima_chapkit) | chapkit | month | — | pass |  |  | 38.6s |
| [`auto_ets`](https://github.com/chap-models/auto_ets) | mlproject | any | — | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 20.1s |
| [`baseline_model_for_sim_study`](https://github.com/chap-models/baseline_model_for_sim_study) | mlproject | month | req: population, rainfall, mean_temperature | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 33.9s |
| [`chap_auto_ewars`](https://github.com/chap-models/chap_auto_ewars) | mlproject | month | req: rainfall, mean_temperature, population | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 130.3s |
| [`chap_auto_ewars_weekly`](https://github.com/chap-models/chap_auto_ewars_weekly) | mlproject | week | req: rainfall, mean_temperature, population | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 61.2s |
| [`chap_pymc`](https://github.com/chap-models/chap_pymc) | mlproject | any | req: mean_temperature, rainfall<br>free | pass |  |  | 398.7s |
| [`chapkit_ewars_model`](https://github.com/chap-models/chapkit_ewars_model) | chapkit | month | req: population<br>acc: rainfall, mean_temperature<br>free | pass |  |  | 313.9s |
| [`chapkit_minimalist_example_py`](https://github.com/chap-models/chapkit_minimalist_example_py) | chapkit | month | — | pass |  |  | 84.0s |
| [`chapkit_minimalist_example_r`](https://github.com/chap-models/chapkit_minimalist_example_r) | chapkit | month | — | pass |  |  | 36.3s |
| [`chapkit_simple_multistep_model`](https://github.com/chap-models/chapkit_simple_multistep_model) | chapkit | month | acc: rainfall, mean_temperature, mean_relative_humidity<br>free | pass |  |  | 34.4s |
| [`chtorch`](https://github.com/chap-models/chtorch) | mlproject | any | req: population<br>free | pass |  |  | 48.4s |
| [`D-FENSE---LNCC-ARp-2025-1`](https://github.com/chap-models/D-FENSE---LNCC-ARp-2025-1) | mlproject | any | — | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 7.7s |
| [`epidemiar_example_model`](https://github.com/chap-models/epidemiar_example_model) | mlproject | any | req: population, precipitation, temperature | fail | nonzero_exit · ⚠ platform=linux/amd64 · data=synthetic | Model's train/predict script failed at runtime. Inspect the run.log for the underlying error — common causes: undeclared covariates, missing R/Python deps, or data-shape assumptions in the model script. | 6.2s |
| [`ewars_per_district`](https://github.com/chap-models/ewars_per_district) | mlproject | any | req: population, rainfall, mean_temperature<br>free | fail | docker_pull_failed · ⚠ platform=linux/amd64 · data=curated+geo | MLproject `docker_env.image` is private, missing, or arch-incompatible. Push the image to ghcr.io public, switch to a chapkit-images base (`chapkit-py` / `chapkit-r` / `chapkit-r-inla`), or pin a multi-arch tag. | 7.1s |
| [`ewars_template`](https://github.com/chap-models/ewars_template) | mlproject | any | req: population<br>free | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 43.5s |
| [`Exponential_smoothing_state_space_model`](https://github.com/chap-models/Exponential_smoothing_state_space_model) | mlproject | any | free | fail | docker_image_missing_runtime · ⚠ platform=linux/amd64 · data=curated+geo | Model's container is missing R / required R packages. Use chapkit-r or chapkit-r-inla as the base image, and `R -e 'install.packages(...)'` (or apt) the missing libraries during build. | 9.1s |
| [`INLA_baseline_model`](https://github.com/chap-models/INLA_baseline_model) | mlproject | any | req: population<br>free | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 43.7s |
| [`LaCiD-UFRN-ARIMAX`](https://github.com/chap-models/LaCiD-UFRN-ARIMAX) | mlproject | any | req: population, mean_temperature | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 54.7s |
| [`Madagascar_ARIMA`](https://github.com/chap-models/Madagascar_ARIMA) | mlproject | month | req: population, precipitation, temperature | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 14.4s |
| [`mean`](https://github.com/chap-models/mean) | mlproject | any | — | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 16.7s |
| [`minimal_template_example`](https://github.com/chap-models/minimal_template_example) | mlproject | any | — | fail | model_runtime_error · data=curated+geo | Model's train/predict script failed at runtime. Inspect the run.log for the underlying error — common causes: undeclared covariates, missing R/Python deps, or data-shape assumptions in the model script. | 6.8s |
| [`rwanda_random_forest`](https://github.com/chap-models/rwanda_random_forest) | mlproject | any | — | fail | model_runtime_error · data=curated+geo | Model's train/predict script failed at runtime. Inspect the run.log for the underlying error — common causes: undeclared covariates, missing R/Python deps, or data-shape assumptions in the model script. | 9.7s |
| [`rwanda_sarimax`](https://github.com/chap-models/rwanda_sarimax) | mlproject | any | — | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 12.7s |
| [`Vietnam-dengue-superensemble`](https://github.com/chap-models/Vietnam-dengue-superensemble) | mlproject | month | req: population, minimum_temperature, maximum_temperature, nino34_anomaly, specific_surface_humidity, wind_speed, periurban_landcover, urban_landcover<br>geo | fail | nonzero_exit · ⚠ platform=linux/amd64 · data=synthetic+geo | Model's train/predict script failed at runtime. Inspect the run.log for the underlying error — common causes: undeclared covariates, missing R/Python deps, or data-shape assumptions in the model script. | 13.8s |
| [`XGBoost_for_Malawi`](https://github.com/chap-models/XGBoost_for_Malawi) | mlproject | month | req: population, rainfall, mean_temperature | pass | ⚠ platform=linux/amd64 | Ran only because the host pre-pulled the docker image with `--platform=linux/amd64`. The image lacks an arm64 manifest, so this would fail on an arm64 deploy. Author should publish a multi-arch image or the deploy needs to force the platform too. | 105.3s |
| [`Xiang_LSTM`](https://github.com/chap-models/Xiang_LSTM) | mlproject | any | — | fail | model_runtime_error · data=curated+geo | Model's train/predict script failed at runtime. Inspect the run.log for the underlying error — common causes: undeclared covariates, missing R/Python deps, or data-shape assumptions in the model script. | 9.3s |
| [`Xiang_SVM`](https://github.com/chap-models/Xiang_SVM) | mlproject | any | — | pass |  |  | 7.9s |
